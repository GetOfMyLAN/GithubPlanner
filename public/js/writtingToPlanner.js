//-------------
//||Functions||
//-------------

//Function that takes a string and a file, and writes it to the end or to the whole file
function writeToFile(file, stringToBeWritten, options){
	$.ajax({})
}
